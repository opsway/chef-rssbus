<% Array(node['rssbus']['ports']).each do | port | %>
  <%= node['rssbus']['dir'] %>/RSSBusApps<%= port %>/webserver/logs/start.log
<% end %>
{
        weekly
        rotate 4
        missingok
        notifempty
        compress
        nocreate
}

